import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['placehold.co', 'tokens.1inch.io', 'raw.githubusercontent.com'],
  },  
};

export default nextConfig;
